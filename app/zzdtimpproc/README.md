# Manage Processor Artifact

This Fiori Elements app maintains `ProcessorArtifact` records from `DataImportService`.

The app is built as an HTML5 module for BTP deployment through `batchimportcat-app-deployer`.
