COLUMN TABLE QRTZ_CALENDARS
(
    SCHED_NAME VARCHAR(120) NOT NULL,
    CALENDAR_NAME  VARCHAR(200) NOT NULL,
    DESCRIPTION VARCHAR(250) NULL,
    CALENDAR BLOB NOT NULL,
    PRIMARY KEY(SCHED_NAME,CALENDAR_NAME)
);